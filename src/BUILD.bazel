common_copts = ["-std=c++20"]

cc_binary(
    name = "main",
    srcs = ["main.cpp"],
    copts = common_copts,
    deps = [
        "@boost.asio",
    ],
)

cc_binary(
    name = "app",
    srcs = ["main.cpp"],
    copts = common_copts,
    deps = [
        "@boost.asio",
    ],
)
